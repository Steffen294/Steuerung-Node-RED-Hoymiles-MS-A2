Installation in Homeassistant:

Folgende HA Add-Ons installieren:
Mosquitto Broker
Node-RED

MQTT einrichten:

Node-RED:
Node-RED Github JSON File in Node-RED importieren:

Die IP Adressen für den MQTT Broker und den Shelly PRO 3EM müssen angepasst werden.
Modbus auf dem Shelly PRO3 EM aktivieren.

Die Seriennummer der Hoymiles Speichers müssen in den MQTT Nodes angepasst werden.

MQTT Einstellungen in der Hoymiles App aktivieren.

Aufruf der Node-RED WEB UI:
http://iphomeassistant:1880/endpoint/ui
